# Homepage revision contract

Authority: user's current homepage change request. This revision supersedes conflicting original requirements; existing content and assets remain authoritative.

Scope: HomePage.tsx, homepage.css, Remix Icon dependency, and task handoffs. No Git delivery or later review stages.

Acceptance: edge-to-edge square section backgrounds, no banner top gap; header/banner/growth/footer inner content capped at 1300px. Smaller fluid type and hero composition. Collapsed header actions precede hamburger; below 640px actions move into a 75vw left drawer with blur backdrop, close button, outside/Escape close and focus management. Drawer exits right as requested. Nav hover underline and active anchor underline. Remix hamburger and arrow icons. Pointer cursors and smooth button transitions. Mobile hero content remains in flow above statistics.

Video: real video elements with controls and existing poster scenes; source files explicitly pending user upload. Three/two/one responsive columns. No fabricated video source. Features wrap and gain green hover shadow. Products retain three distinct identities and three desktop visible items; repeat existing items only to support continuous Swiper loop, autoplay and hover pause at all breakpoints. Side arrows, raised darker shadowed labels. Reduced motion disables autoplay. Every reason card receives former sixth-card hover/focus behavior. Growth panels equal height and stack below 768px. Footer reduces sizing, wraps below 768px and becomes one column at 540px; social hover dark green; copyright/legal links use # and brighten on hover.

Reuse established design/asset evidence. Checks: typecheck, lint, build, targeted runtime responsive/menu/carousel/media DOM checks. Record actual results and unavailable playback separately. Implementation checks do not imply independent Verification or Visual QA.
